# Files Labelled

* **generate_dataset**: this generates the dataset in the folder `ptrnet_dataset` in two formats. the `jsonl` holds the hamiltonian path zero-indexed based off of the grid. tried to make it as close to the prtnet paper
* **hamiltonian_env**: this is the actual puzzle. 
* **visualize_puzzle**: this visualizes the puzzle and is used in `visualize_dataset`
* **visualize_dataset**: this will load the dataset and visualize the first one.
